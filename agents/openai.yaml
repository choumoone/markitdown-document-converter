interface:
  display_name: "MarkItDown Document Converter"
  short_description: "Convert documents, Markdown, HTML, and DOCX publishing outputs."
  default_prompt: "Use $markitdown-document-converter to convert this document folder into clean Markdown, then publish to HTML or DOCX if needed."
