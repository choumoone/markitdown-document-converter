interface:
  display_name: "MarkItDown Document Converter"
  short_description: "Convert documents with local Qwen OCR delegation"
  default_prompt: "Use $markitdown-document-converter to convert these documents and automatically delegate scanned pages to local Qwen OCR."
