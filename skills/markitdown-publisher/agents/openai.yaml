interface:
  display_name: "MarkItDown Publisher"
  short_description: "Publish Markdown with overwrite safeguards"
  default_prompt: "Use $markitdown-publisher to report the publishing scope and request authorization when output was not explicitly requested."
