interface:
  display_name: "MarkItDown OCR"
  short_description: "OCR scans with explicit authorization gates"
  default_prompt: "Use $markitdown-ocr to report the OCR scope and request authorization before processing these scans."
